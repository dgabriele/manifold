// Expression helpers for the planner.
//
// Currently the core lowering logic lives in `super::lower_expr`.
// This module is reserved for future helper functions (e.g., constant folding,
// expression simplification) that operate on `ScalarExpr`.
